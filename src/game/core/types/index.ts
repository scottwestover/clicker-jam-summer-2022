export type LevelConfig = {
  functionText: string;
  functionTests: string[];
  taskText: string;
  storyPoints: number;
  experienceReward: number;
};

export type UpgradeConfig = {
  baseCost: number;
};
